# 迁移方案：大类 → 主分组（价目表替换线上）

| 字段 | 内容 |
|------|------|
| 文档版本 | v1.1 |
| 日期 | 2026-09-23 |
| 状态 | **决策已拍板，待评审排期**（本文只定方案，不含原型改码） |
| 涉及模块 | 价目表（项目创建与管理）· 提成设置 · 开单选品 · 会员卡权益选品 · 库存 · 经营分析 |
| 关联文档 | `PRD-项目创建与管理.md`（v1.27）· `../提成设置/PRD-提成设置.md` |
| 结论 | 线上「大类」升级为**「主分组」**（必选、1:1、**可改**）+ 新增**可多归属的自定义分组**；提成解析由实测的**「先写先赢」**改为**显式优先级链**；分组变更**不弹窗、自动 `forward`、仅一条 toast**；**一次性全量替换**，靠上线前体检 + 回滚包兜底。 |

---

## 修订记录

| 版本 | 日期 | 内容 |
|------|------|------|
| v1.1 | 2026-09-23 | **① 分组变更改为「不弹窗、自动 `forward`、仅 toast」** —— 原 D4「复用两枚按钮（本期全部重算 / 从现在起用新方案）」调整为：分组归属变更**即时生效**，固定走 `forward`（分界时刻之后按新分组算），**不再让用户二选一**；仅当「被变更的分组」∩「被提成规则引用的分组」≠ ∅ 时弹一条 toast（文案 `分组已更新 · 之后的提成按新分组计算`），未命中则静默；删组改为 `「X」已删除 · N 条提成规则不再生效`。改写 §2（D4）· §6（护栏 → 变更提示）· §7（生效口径重写）· §10（M12/M13 改写 + 新增 M17–M19）。**② 明确「分界记录」为必需项** —— 因薪资侧 `employee.js` 的 `finalizeSeedCommLines()` 是**重算**（`calcStaffTrial(staffId, trialPayload, state.salaryMonth)`）而非读存量，且 `invalidateCommLineCache()` 在仓库中被调用 30 余处，故只落 toast 不落分界会导致**当期金额被静默重算**、toast 文案失真。给出**低成本实现**：分组新增 `_prevItemIds` + `effectiveAt`，`lineInGroup()` 按 `line.ymd` 取值（**复用方案侧既有 `_prev` 模式**，零新增 UI；`line.ymd` 已在试算载荷里）。新增 §3.1 两字段 / §3.3 不变量 6 / §7.1。**③ 明确与方案侧重算弹窗的关系** —— 方案规则改动仍走既有 `#comm2RecalcMask`（二十七～三十二次定稿）**本次不动**；两条链各自独立、按行日期可组合（新增 §7.3 对照表）。**④ 新增 §11「术语统一：类 → 组」** —— **范围经用户澄清后收窄**：改**只针对**「通过价目表编辑的项目 / 产品」及「价目表功能所产生的分类 / 分组行为」这一条轴，**不涉及任何其他含义的「类」**；新增 §11.0 适用范围与一句话判定准则、§11.1 改名对照表（含 **`comm2.js` L1176 注释 / L1181 / L2600**）、§11.2 排除清单（`#comm2CatSheetTitle`「分类提成」L425 与帮助弹窗「大类 / 大类卡」L471 / L481 / L574 —— 后两者由同弹窗 L470「每张卡管一类业务」、L478「这单属于哪一类」自证属**业务类型轴**；支出管理「分类管理」）、§11.3 歧义项（`comm2.js` L2605「该分类下暂无内容」两种读法 + 中性文案建议）、§11.4 执行方式；验收同步 M20 / 新增 **M21（排除项未被误改）/ M22（歧义项）**。**⑤ 勘误**：v1.0/v1.1 初稿曾把帮助弹窗的「大类 / 大类卡」列入改名范围，经核 L470 / L478 上下文后**判定为业务类型轴、移入排除清单**。 |
| v1.0 | 2026-09-23 | 首版：现状核对（三边差异 + 实测证据 + 风险清单）· 7 项决策记录 · 目标数据模型与不变量 · P0 数据迁移（映射 / 回滚包 / `category` 兼容期）· P1 提成优先级链 · P2 变更护栏 · P3 生效时点 · P4 下游对齐 · 上线预案与验收 M1–M16。 |

---

## 1. 问题与事实

### 1.1 三边现状

| 维度 | 线上（现网） | 原型（本仓库） | 提成设置 |
|------|--------------|----------------|----------|
| 归属概念 | **大类**（项目分类 / 产品分类） | **自定义分组** + 系统「隐藏」组 | 「整组」= 分组（`{kind:'group'}`） |
| 基数 | 必选 | 可选、可空（条目可为 0 个分组） | — |
| 重数 | **1:1**（一个条目属于 1 个大类） | **N:N**（可属多组） | 规则可命中多条 |
| 可改 | 不可改（前线诉求「大类选错可改」） | 随时可改（含全部取消） | — |
| `category` 字段 | 有效、用户可见 | **仍在数据模型里，但写死常量**（项目「其他」/ 产品「产品」） | — |
| 解析规则 | — | — | 实测 **先写先赢**（见 §1.2） |

> 线上「分组选择」（`ProjectGroupSelectFragment`）经确认**只是大类的另一个叫法，同一份数据** —— 因此线上**不存在**"多归属分组"这一既有能力；原型引入的是**全新能力**，迁移是干净的 1:1 映射，无需合并两套分组。

### 1.2 实测证据：提成不是「取高」，是「先写先赢」

直接调用 `Comm2Demo.calcStaffTrial` 打真实试算链路（试算行 `深层补水护理` / `p21` / 原价 268 / 员工 `st0`）：

| 场景 | 覆盖项书写顺序 | 实际计提 | 生效费率 |
|------|----------------|---------:|---------:|
| A 两条覆盖都指向**同一个具体项目** | 5% 在前，20% 在后 | 13.4 | **5%** |
| A 同上 | 20% 在前，5% 在后 | **53.6** | **20%** |
| B 两条覆盖各指向一个**分组**（项目同属两组） | 5% 在前 | 13.4 | **5%** |
| B 同上 | 20% 在前 | **53.6** | **20%** |
| C **分组**覆盖(40%) vs **具体项目**覆盖(5%) | 分组在前 | **107.2** | **40%** |
| C 同上 | 项目在前 | 13.4 | **5%** |

**结果完全随书写顺序翻转。** 根因（`提成设置/comm2.js`）：

```text
resolveLineBlock(sch, line)   // L1620
  for (ov of sch.overrides)                 // 顺序遍历
    for (t of ov.targets)
      if (t.kind === 'group' && lineInGroup(line, t, gk)) return ov;   // 第一条命中即返回
```

- 「取高」**只存在于跨方案层**：`calcStaffTrial` 里 `if (!winner || c.amount > winner.amount)`（L1866，严格大于 → 并列取列表中更靠前的方案）。
- 用户新增规则项走 `sch.overrides.push(...)`（L2377，**追加到末尾**）；`unshift` 仅用于系统「快捷开单」（L342，故意置顶）。
- 覆盖列表**无拖拽排序、无命中来源显示、无冲突提示**。

**连带隐患**：删除分组后，指向它的规则 `lineInGroup` 里 `find` 不到 → **静默不命中 → 回落默认卡**，金额可升可降，全程无提示。

**与文档的矛盾**（需一并修正）：PRD / 帮助弹窗写的「好几张卡都管到同一个项目 → 挑钱多的那张算」「单独加的规则项更优先」—— C 场景证明**覆盖之间的"更优先"不成立**。

### 1.3 若不做本方案会发生什么

| 后果 | 说明 |
|------|------|
| 分类塌缩 | 按原型直上，存量 `category` 全部写入「其他」/「产品」，报表与导入导出失去维度 |
| 提成真空 | 分组可空 → 出现无归属条目，提成从"必有类规则覆盖"退化为可能只吃默认卡 |
| 提成不可解释 | 多归属使重复命中概率大增，而规则是"先写先赢" → 改费率"没反应"、加规则"不生效" |
| 无声改钱 | 归属从"改起来很麻烦"变成"点两下"，且删组会静默废掉规则 |

---

## 2. 决策记录（本次拍板）

| # | 议题 | 决策 |
|---|------|------|
| D1 | 大类去留 | **大类 → 「主分组」**：必选、1:1、**可改**，用于统计 / 导入 / 默认筛选；另可自由加**自定义分组**（多归属） |
| D2 | 重复命中规则 | **显式优先级链**：具体项目/产品 > 分组 > 默认卡；**同层取高**；并列按规则项顺序 |
| D3 | 变更护栏 | **允许改，仅 toast 轻提示**（不阻断、不弹确认） |
| D4 | 分组变更的生效口径 | **不弹窗、即时生效、固定 `forward`**：分界时刻**之后**按新分组算；仅一条 toast 告知（文书 §7）。**不再**复用"本期全部重算 / 从现在起用新方案"两枚按钮 —— 那条路径属**方案规则改动**（D8），本决策不涉及 |
| D5 | 线上「分组选择」 | 即大类的同义叫法，**同一份数据**；迁移为 1:1 |
| D6 | 下游依赖 | 经营分析/报表 · 开单选品 · 会员卡权益选品 · 库存（**导入导出、商城/联盟侧本次判定为不依赖**） |
| D7 | 上线方式 | **一次性全量替换** |
| D8 | 方案规则改动的重算弹窗 | **保持不动** —— 方案 `defaults` / `overrides` / `stationIds` / `stationLabels` 改动仍走既有 `#comm2RecalcMask`（两枚竖排按钮，二十七～三十二次定稿） |
| D9 | 术语口径 | 替换上线后，其他地方关于项目/产品「**类**」的描述统一改为「**组**」（§11） |

> **D3 + D7 的代价**：没有灰度观察期、又没有阻断，因此**上线前体检（§9.1）+ 回滚包（§9.2）+ 上线后核对（§9.3）是硬性补偿项**，不可省略。
>
> **D4 的代价**：分组变更**没有"重算历史"的退路**，且用户**无法在不生效的前提下试改**。因此「分界记录」是**必需项而非可选项**（§7.1 给出理由与实现）。

---

## 3. 目标数据模型

### 3.1 分组 `CatalogGroup`（在原型基础上扩展）

| 字段 | 类型 | 新增 | 说明 |
|------|------|:----:|------|
| `id` | string | | 桶内唯一 |
| `name` | string | | 桶内唯一；系统组不可改名 |
| `itemIds` | string[] | | 组成员 |
| `order` | number | | 轨内顺序 |
| `system` | boolean | | 系统组：不可改名 / 不可删 / 不参与提成目标 |
| `systemKind` | `'hidden' \| 'fallback'` | ✅ | `hidden` = 隐藏组；`fallback` = 主分组兜底组（§3.3） |
| `source` | `'category' \| 'custom'` | ✅ | 迁移来源标记（仅作追溯与回滚用，UI 不展示） |
| `_prevItemIds` | string[] \| null | ✅ | **归属分界 · 变更前成员快照**（D4）。仅在**成员集发生变化**时写入；无变更时为 `null` |
| `effectiveAt` | string \| null | ✅ | **归属分界时刻**（`YYYY-MM-DD`，与订单行 `ymd` 同格式）。`ymd < effectiveAt` 的行按 `_prevItemIds` 解析，`>=` 按 `itemIds` 解析 |
| `effectiveKind` | `'member'` | ✅ | 分界来源（当前只有成员变更一种；为后续扩展留位） |

> **与方案侧 `_prev` 的分工**：方案侧 `_prev` 快照的是**规则集**（`defaults` / `overrides` / …），分组侧 `_prevItemIds` 快照的是**成员归属**。两者**同模式、可叠加**：同一行先按 `schForLine` 取当时方案，再按 `lineInGroup` 取当时成员集（§7.3）。

桶结构不变：`state.catalogGroups = { project: [...], product: [...] }`。

### 3.2 项目 / 产品（在原型基础上扩展）

| 字段 | 类型 | 说明 |
|------|------|------|
| `primaryGroupId` | string | ✅ **新增**。主分组 id；**非空**，且**必须 ∈ `groupIds`** |
| `groupIds` | string[] | 归属的自定义分组集合（含 `primaryGroupId`；**不含**系统「隐藏」组） |
| `category` | string | 迁移期**保留只读兼容**（§4.3），新写入不再依赖；下线时机见 §11 |
| `hidden` | boolean | 保持与系统「隐藏」组成员双向同步（不变） |

### 3.3 不变量（Invariants，必须由服务端与前端共同保证）

1. `primaryGroupId` **恒非空**且**恒存在于同桶分组表**。
2. `primaryGroupId ∈ groupIds`（勾选里取消不掉主分组；取消主分组需先改指另一个）。
3. 每个桶**至少存在一个** `systemKind:'fallback'` 分组（项目「其他」/ 产品「产品」），**不可删除、不可改名、不可设为隐藏**。
4. 系统「隐藏」组**不入** `groupIds`、**不可**作为主分组、**不可**作为提成「整组」目标（现 `getCustomCatalogGroups()` 已按 `!g.system` 过滤，保持）。
5. 删除分组时，若它是某些条目的主分组 → **必须先指定迁入分组**（默认指向 fallback 组），再删；指向它的提成规则**必须提示**（§6）。
6. **归属分界单调**：`effectiveAt` 一旦写入即为**当前分界点**，后续再改会**覆盖它**（不做多段历史）。写入时必须同时落 `_prevItemIds`（覆盖前成员集）与 `effectiveAt`（本次变更时刻），二者**同生同灭** —— 单独存在任一项即为脏数据。
7. **迁移初始**：`_prevItemIds = null` / `effectiveAt = null`（迁移本身是"无前史"的起点，不产生分界，不触发 toast）。

### 3.4 映射（线下 → 新模型，1:1）

| 线上 | 新模型 |
|------|--------|
| 大类 `c` | 分组 `{ id: 'g_'+c.id, name: c.name, order: c.order, source: 'category' }` |
| 条目 `it.category === c` | `it.primaryGroupId = 'g_'+c.id`；`it.groupIds = ['g_'+c.id]` |
| 无分类/空分类条目 | 指到本桶 fallback 组 |
| 线上「未分类」占位（若有） | 与 fallback 组合并，不新建 |

---

## 4. P0 数据迁移

### 4.1 步骤（幂等、可回滚）

1. 冻结写入（价目与提成两侧短时只读窗口）。
2. 为每个桶建分组表：先建 fallback 组，再按大类顺序建 `source:'category'` 组（**保留原排序与名称**）。
3. 回填 `primaryGroupId` / `groupIds`（按 §3.4）。
4. **校验通过后才切读**：见 §9.1 体检项全绿。
5. 解冻。

### 4.2 回滚包（一次性全量的必需品）

- 保留 `category` 字段原值（不覆盖），并落一份 `迁移映射表`（`groupId ↔ categoryId`）。
- 保留迁移前价目全量快照（条目 + 大类 + 排序）。
- 回滚动作：切回读 `category` + 隐藏分组入口；`primaryGroupId`/`groupIds` 保留但忽略。**回滚不需要反向写数据**，因此是双向安全的。
- 提成侧回滚：`source:'category'` 分组与原型分组同构，规则无需回滚；但若已按 §5 上线新解析，需一并回退代码版本。

### 4.3 `category` 字段兼容期

- 迁移后 `category` **只读保留**，不参与任何计算与展示（除 §9.3 核对）。
- 下线时机：**至少跨过一个完整结算周期**，且在 §9.3 抽样核对全绿之后（见 §11 待确认）。

---

## 5. P1 提成优先级链（替换「先写先赢」）

### 5.1 分层

| 层 | 目标类型 | 说明 |
|---:|----------|------|
| L1 | `{kind:'project' \| 'product' \| 'card'}`（具体标的） | 含系统「快捷开单」（`refId=COMM2_QUICK_REF`，保持最高优先） |
| L2 | `{kind:'group', groupKind}`（分组） | 「整组」 |
| L3 | `sch.defaults[cat]`（默认卡） | 兜底 |

### 5.2 解析算法（替换 `resolveLineBlock` 的单命中语义）

```text
candidates = []                       // 收集，不再 return
for ov in sch.overrides (index asc):
  for t in ov.targets:
    tier = (t.kind === 'group') ? 2 : 1
    if matches(t, line):               // 项目/产品/卡 或 lineInGroup
      candidates.push({ ov, tier, index })

if candidates empty:
  if isQuickLine(line): return 系统快捷开单覆盖
  return sch.defaults[cat]             // L3

minTier = min(candidates.tier)         // 取最具体的那一层
tiered  = candidates.where(tier === minTier)

eligible = tiered.where(试算可计提)     // scope 通过（§5.3.1）
if eligible empty: return tiered 首条    // 保持现状：命中即不回落默认卡
winner = eligible 中 amount 最大者；并列取 index 最小者
return winner.ov
```

要点：
- **分层的判据是"目标具体程度"**，与费率高低、添加先后**无关** —— 这直接修掉 §1.2 的 C 场景（分组 40% 再也压不过具体项目 5%）。
- **同层取高**用**试算金额**比较（不是费率），与跨方案层一致；并列按规则项顺序，与现文档「钱一样多时按先后」自洽。
- **跨方案层不动**（`calcStaffTrial` 的严格大于 + 并列取靠前方案）。
- 需要在 `schemeLineAmount` 内改为"对候选集逐块试算再选"，即把现有单块计算抽成 `amountOfBlock(sch, block, line)` 复用（`sign` 分支同样要走）。

### 5.3 边界口径

#### 5.3.1 命中但适用范围不符
保持现状语义：**命中即不再回落默认卡**。同层取高只在**可计提候选**间进行；若该层全部候选不可计提 → 结果为 0（skipped:`scope`），并在规则卡上标注「命中但不计提」。→ 避免"改一条规则的适用范围导致金额反而变高"。

#### 5.3.2 系统「快捷开单」
系统覆盖项**恒为 L1 且 index 最小**（保持 `unshift` 语义），不吃「项目」默认卡。

#### 5.3.3 系统「隐藏」组
不作为提成目标出现（`getCustomCatalogGroups` 的 `!g.system` 已保证）；迁移后仍须保证（`systemKind:'fallback'` 的兜底组同理）。

#### 5.3.4 同一规则项内多目标
`ov.targets` 内部命中多个目标不产生多份候选（同一 `ov` 只入列一次，取**最高 tier**）。

### 5.4 界面可见化（配合 D2 的"可解释"要求）

1. **规则卡标题**：显示命中来源，如 `整组·烫染` / `深层补水护理`（现已有，术语按 §11 统一）。
2. **方案编辑页**：对同一标的被 ≥2 条规则命中的情况，在相关规则卡上加角标「**与其他 N 条规则重叠，按费率/金额取高**」，点开列出重叠项与最终生效者。
3. **试算页**：每行显示「生效来源 = {规则名}（{层}）」+ 「被 N 条规则覆盖」明细。
4. **帮助弹窗**：把「好几张卡都管到同一个项目 → 挑钱多的那张算」改写为准确表述 ——
   **「先看有没有给这个项目单独设规则；有就按单独的算（几条单独规则之间挑钱多的）。没有单独的，再看分组规则（几条分组规则之间挑钱多的）。都没有，才按默认卡算。钱一样多时，按规则项添加的先后。」**

### 5.5 与「跨方案取高」的关系

| 层 | 规则 | 变更 |
|----|------|------|
| 方案内 | 优先级链（L1 > L2 > L3），同层取高 | **本次修改** |
| 方案间 | 金额最大者；并列取列表靠前方案 | 不变 |

---

## 6. P2 分组变更提示（D3 + D4：不弹窗、仅 toast）

### 6.1 总原则

分组归属变更（含成员增删、删组、改主分组）**一律即时生效、无弹窗、无二选一**；是否提示**取决于该分组是否被提成规则引用** —— 未被引用则**完全静默**（改一个没人用的分组不该打扰用户）。

### 6.2 触发矩阵与文案

| 入口 | 弹 toast 的条件 | 文案 |
|------|----------------|------|
| 包含项目 / 包含产品页 · 确定 | 本次变化的**任一**分组被 ≥1 条提成规则引用 | `分组已更新 · 之后的提成按新分组计算` |
| 条目详情 · 改主分组 | 主分组（旧或新）被 ≥1 条提成规则引用 | 同上 |
| 分组管理 · 删除分组 | 该分组被 ≥1 条提成规则引用 | `「X」已删除 · N 条提成规则不再生效` |
| 分组管理 · 重命名 | **从不**（id 不变，提成不受影响） | — |
| 分组管理 · 新建分组 | **从不**（新组尚未被引用） | — |
| 任一入口 · 分组未被任何规则引用 | **从不** | — |

**文案定稿（简明扼要，与现有 toast 同规格）**

- 成员变更 / 主分组变更：**`分组已更新 · 之后的提成按新分组计算`**（16 字，单行）
- 删组：**`「X」已删除 · N 条提成规则不再生效`**（N = 引用该组的规则项数）

> 两条文案都**只说结论、不说原因**，且**不出现"本期"字样** —— 因为生效粒度是**按单据日期**而非"按期"（§7.1），写"本期"会与"已结的单不改"冲突（见 §7.2）。

### 6.3 残余风险与强制补偿

轻提示**不阻断**，因此以下两点为**强制**补偿：

1. **删组保护**：若该分组被提成规则引用，删除确认弹窗必须**列出规则名**（一次性、可跳过，但必须出现一次）。
2. **规则失效可观测**：进入方案编辑页时，对"引用了已删除分组"的规则项标红并提示「目标分组已不存在，该规则不会命中」—— 修掉 §1.2 的静默失效隐患。

> 与旧版（v1.0）的差别：原「包含项目/产品页确定」文案 `已更新成员 · N 个项目的提成将变化` **作废** —— 它只说了"会变"、没说"从何时起"，且"将变化"与"即时生效"语义冲突。

---

## 7. P3 生效口径（D4：不弹窗、自动 `forward`）

### 7.1 为什么「分界记录」是必需项

**关键事实**：薪资侧不是"读存量"，而是**每次缓存失效都重算** —— `提成设置/employee.js`：

```text
finalizeSeedCommLines(lines, staffId)
  → C.calcStaffTrial(staffId, trialPayload, state.salaryMonth)      // L3511-3512
  → 每行载荷带 { ymd: ln.ymd }                                      // L3509（二十六次为 forward 加的）
```

而 `invalidateCommLineCache()` 在仓库中被调用 **30 余处**（切月份 / 员工变更 / 方案保存 / 归属变更…）。

因此：**若只加 toast 而不落分界，之后任何一次缓存失效都会用新分组把当期金额静默重算** —— toast 说的"之后的提成按新分组算"当场失真，且用户不会察觉。

**实现（复用方案侧既有 `_prev` 模式，零新增 UI）**

```text
lineInGroup(line, target, groupKind):
  g = comm2Groups(groupKind).find(x => x.id === target.refId)
  if (!g) return false
  members = (g.effectiveAt && line.ymd && line.ymd < g.effectiveAt)
              ? (g._prevItemIds || [])
              : (g.itemIds || [])
  return members.indexOf(line.refId) >= 0
```

- 改动面 = **1 个函数 + 2 个字段 + 3 处写入点**（包含项目/产品页确定 / 删组 / 改主分组）
- `line.ymd` **已在试算载荷里**（二十六次为方案侧 `forward` 引入），无需新增管线
- 与 `schForLine(sch, periodKey, ymd)` **同思路、可叠加**（§7.3）

### 7.2 生效粒度与边界

| 情形 | 口径 |
|------|------|
| 分界时刻**之后**的单据 | 按**新**分组算 |
| 分界时刻**之前**的单据 | 按**旧**分组算（`_prevItemIds`） |
| 已结算 / 历史期 | **不回算**（与 v1.0 的"迁移当天不回算"一致，继续成立） |
| 人工改过的提成值 | **恒优先**，任何重算都不覆盖（沿用现状：`_manualPreserve`） |
| 迁移当天 | `effectiveAt = null` → 不产生分界 → 全部按迁移后分组表算；迁移**不弹** toast |
| 连续多次改动 | **覆盖**上一个分界点，不做多段历史（§3.3 不变量 6 已标注） |

### 7.3 与方案侧重算弹窗的关系（D8，互补不冲突）

| 改动类型 | 机制 | 用户交互 |
|----------|------|----------|
| **分组归属**（成员 / 主分组 / 删组） | 分组侧 `_prevItemIds` + `effectiveAt` | **无弹窗**，仅 toast（§6.2）；固定 `forward` |
| **方案规则**（`defaults` / `overrides` / `stationIds` / `stationLabels`） | 方案侧 `_prev` + `effectiveMode` + `effectiveAt`（二十七～三十二次定稿） | 既有 `#comm2RecalcMask` 两枚竖排按钮（`recalc` / `forward`）**本次不动** |

两者**按同一行日期独立解析后叠加**，一行金额的实际取数链：

```text
calcStaffTrial(staffId, lines, periodKey)
  → 每行: schForLine(sch, periodKey, line.ymd)     // 方案侧：老/新规则集
  →       schemeLineAmount(sch, line)
  →         resolveLineBlock(sch, line)            // §5 优先级链（本次修改）
  →           lineInGroup(line, t, groupKind)      // 分组侧：老/新成员集（§7.1）
```

即：**方案侧决定"用哪套规则"，分组侧决定"这条规则管不管这个项目"，两者各按行日期取值，互不干扰**。

---

## 8. P4 下游对齐清单（D6）

| 下游 | 现状 | 迁移要求 |
|------|------|----------|
| 经营分析 / 报表 | 按大类的统计维度 | 维度键由 `category` 改为 `primaryGroupId`；报表口径默认用**主分组**，自定义分组不进默认报表（可后续开放多维） |
| 开单选品 | 分组轨「全部 / 自定义 / 隐藏」 | 轨来源改为分组表；**隐藏组仍可选**（`onSale` 含 hidden，口径不变） |
| 会员卡权益选品 | `onSale && !hidden` | 不受分组影响，仅需回归验证（选择器若含分组轨，与开单一致） |
| 库存（挂在价目增改） | 跟价目条目走 | 不依赖归属，仅需回归验证库存入口未随表单字段调整而错位 |
| 导入导出 · 商城/联盟 | 本次判定**不依赖** | 若实际存在按大类的模板/接口，需回补映射（§11） |

---

## 9. 上线预案（D7：一次性全量）

### 9.1 上线前体检（dry-run，必须全绿）

1. **归属完整性**：`primaryGroupId` 非空率 = 100%；`primaryGroupId ∈ groupIds` 违反数 = 0。
2. **映射一致性**：条目数、名称、价格、`onSale`、`hidden`、排序，迁移前后逐条 diff = 0。
3. **分组数核对**：新分组数 = 原大类数 + 1（fallback/桶）。
4. **提成影响报告**（最重要）：对全部方案重放试算，输出
   - 因优先级链改变而**金额变化**的（规则项, 试算行, 变更前 → 变更后）；
   - 因分组可空而**从"类规则命中"退化为"默认卡"**的条目清单；
   - **引用了不存在分组**的规则项（即已静默失效者）。
   此报告需**逐条人工确认**后放行。
5. **系统组隔离**：fallback 组与「隐藏」组均不出现在提成「整组」可选目标中。

### 9.2 回滚触发线

| 指标 | 阈值 | 动作 |
|------|------|------|
| 提成金额与体检报告预测不符的行数 | > 0 | 立即回滚 |
| 归属完整性 / 映射一致性任一项不全绿 | > 0 | 不切读 |
| 上线后 24h 内「提成金额」客诉 | ≥ 1 起 | 评估回滚 |

### 9.3 上线后核对与监控

- T+0：抽样 20 家门店，逐条核对主分组与提成生效来源。
- T+1 / T+7：全量重放试算，与体检报告比对，偏差 = 0。
- 监控：`primaryGroupId` 空值率、指向不存在分组的规则项数、提成人工修正率（环比突增即为异常信号）。
- `category` 字段保留期内，每日 diff「`category` 映射结果 vs `primaryGroupId`」，发现漂移即告警。

---

## 10. 验收项

| # | Given | When | Then |
|---|-------|------|------|
| M1 | 迁移完成 | 查任一条目 | `primaryGroupId` 非空且 ∈ `groupIds`；分组成员与主分组一致 |
| M2 | 迁移完成 | 对比迁移前后全量价目 | 名称/价格/在售/隐藏/排序逐条一致，差异 = 0 |
| M3 | 分组管理页 | 查看系统组 | 存在 fallback 组（项目「其他」/ 产品「产品」），**不可删除、不可改名**；「隐藏」组行为不变 |
| M4 | 某条目为主分组 | 在包含项目/产品页取消勾选它 | `primaryGroupId` **不被取消**；改主分组需走显式入口 |
| M5 | 某分组被 ≥1 条提成规则引用 | 删除该分组 | 先弹确认并**列出规则名**；删除后相关规则项标红并提示「目标分组已不存在」 |
| M6 | 一条目同属两组，两组各有规则（5% / 20%） | 交换两条规则的添加先后 | 计提金额**不随顺序变化**（旧版随顺序翻转，见 §1.2 B） |
| M7 | 分组规则 40% + 同项目单独规则 5% | 按任意顺序配置 | **恒取 5%**（L1 > L2；旧版随顺序在 5%/40% 间翻转） |
| M8 | 同层两条规则，费率 5% / 20% | 任意顺序 | 恒取 20%（同层取高） |
| M9 | 同层两条规则，金额并列 | 任意顺序 | 取规则项顺序靠前者（与跨方案层一致） |
| M10 | 某候选命中但适用范围不符 | 计算 | 不回落默认卡，结果为 0，规则卡标注「命中但不计提」 |
| M11 | 系统「快捷开单」 | 任意配置 | 恒为最高优先，不吃「项目」默认卡 |
| M12 | 包含项目/产品页点确定，且**被变更的分组被 ≥1 条提成规则引用** | 完成 | **无弹窗**；toast `分组已更新 · 之后的提成按新分组计算`；分组立即生效 |
| M12b | 同上，但**被变更的分组未被任何规则引用** | 完成 | **无 toast、无弹窗**，完全静默 |
| M12c | 分组管理 · 重命名 / 新建分组 | 完成 | **从不**弹 toast（id 不变 / 新组未被引用） |
| M13 | 归属变更发生在账期内（分界日 = 今天） | 对比分界**前**与**后**的订单行 | 分界前的行按**旧**成员集算，分界后的行按**新**成员集算；两者**并存且都可复现**；再次触发缓存失效（切月份 / 员工变更 / 重进薪资）后**结果不变** |
| M13b | 分组成员未被改动 | 查看该分组 | `_prevItemIds` 与 `effectiveAt` **均为 `null`**（不写脏分界） |
| M14 | 迁移当天 | 查历史期提成 | 与迁移前完全一致（不回算）；且迁移**不弹** toast、不写 `effectiveAt` |
| M15 | 报表 · 开单选品 · 会员卡权益 · 库存 | 迁移后回归 | 分组维度改为按主分组；隐藏口径、开单可选范围、权益选品范围均与迁移前一致 |
| M16 | 提成「添加规则项」 | 打开目标选择 | fallback 组与「隐藏」组**均不出现**在可选目标中 |
| M17 | 分组归属变更 | 打开方案编辑页 | **不出现** `#comm2RecalcMask`（分组链不再复用该弹窗） |
| M18 | 方案规则改动（`defaults` / `overrides` 指纹变化） | 保存 | **仍正常弹出** `#comm2RecalcMask` 两枚按钮（D8：方案链不动） |
| M19 | 分组已被删、但方案里仍有指向它的规则项 | 打开方案编辑页 | 该规则项**标红**并提示「目标分组已不存在，该规则不会命中」 |
| M20 | 按 §11.1 对照表检索**价目表归属轴**的「类」 | 逐条核对落点 | 全部改为「组」：主分组 / 整组 / 项目组 / 产品组 / 分组（含 `comm2.js` **L1176 注释**、**L1181**、**L2600**） |
| M21 | 按 §11.2 排除清单检索**其他轴**的「类」 | 逐条核对 | **全部保持原文**：`#comm2CatSheetTitle`「分类提成」（L425）、帮助弹窗「大类 / 大类卡」（L471 / L481 / L574）、支出管理「分类管理」—— 一处都不得被误改 |
| M22 | §11.3 歧义项「该分类下暂无内容」（`comm2.js` L2605） | 现场确认指向哪条轴后落地 | 若属分组轨 → 「该分组下暂无内容」；若属类型 Tab → 不改；**或**统一改中性文案「暂无内容」（两种读法下均正确） |

---

## 11. 术语统一：类 → 组（D9）

> **适用范围（边界收窄）**：仅限「**通过价目表编辑的项目 / 产品**」及「**价目表功能所产生的分类 / 分组行为**」这一条轴；**不涉及任何其他含义的「类」**。完整边界、判定准则、对照表、排除清单与歧义项见 **§11.0 – §11.4**。

### 11.0 适用范围（**边界收窄，先看这条**）

本次「类 → 组」的改名**只覆盖一件事**：

> **通过价目表编辑的项目 / 产品**，以及**价目表功能所产生的「分类 / 分组」行为**。

**不涉及任何其他含义的「类」。**

**判定准则（一句话自检）**：这个「类」指的**是不是**「价目表里的项目 / 产品归到了哪个组」？

- **是** → 改（§11.1）
- **否**（是业务类型、人员类别、支出科目、类型 Tab 等任何别的轴）→ **不改**（§11.2）

> 这条边界按用户澄清落定：改名**不是**全局文本替换。下文每一行落点都按此准则筛过；筛不掉的歧义项单列在 §11.3。

### 11.1 改（全部命中"价目表归属"这一条轴）

| 现用表述 | 改为 | 说明 / 精确落点 |
|----------|------|----------------|
| 大类 | **主分组** | 线上表单选项、报表维度、导入模板列名、文档 |
| 项目分类 / 产品分类 | **项目组 / 产品组** | 同上；选择器标题亦同 |
| 分类 / 归类（作为**价目表归属**维度时） | **分组 / 归属** | 价目表 PRD §1.5 能力映射表的「分类/归类」一行 |
| 「分类写入默认「其他」」 | 「**主分组**默认写「其他」」 | 价目表 PRD §6.2 |
| 「分类默认「产品」」 | 「**主分组**默认「产品」」 | 价目表 PRD §6.3 |
| 整类 /「整类·烫染」 | **整组** /「整组·烫染」 | `提成设置/comm2.js` **L1181**（覆盖卡标题 `'整类·' + name`）、**L2600**（选择页列表「整类 · {组名}」）；**L1176**（注释「整类优先」同步改）—— 这两处的「类」指向的正是 `{kind:'group'}` 的**价目表分组** |
| 按大类的统计维度 | 按**主分组**的统计维度 | 经营分析 / 报表（§8） |

> 注：`价目表/` 原型的**用户可见文案本已用「组」**（`catalog.js` / `base.css` / `index.html` 全仓无「分类 / 大类」命中），所以 §11.1 的**代码落点全部在 `提成设置/`** —— 因为只有提成模块在描述价目表分组时用了「类」这个词。

### 11.2 明确**不改**（属"其他类型的类"，误改即语义错误）

| 表述 | 位置 | 为什么不能改 |
|------|------|--------------|
| **「分类提成」** | `提成设置/index.html` **L425**（`#comm2CatSheetTitle`） | 指**默认卡的业务类**（项目 / 产品 / 办卡 / 充卡 / 快捷开单，即内部 `cat`），与价目表归属无关；改成「分组提成」即为**语义错误** |
| **「大类」「大类卡」** | `提成设置/index.html` **L471 / L481 / L574**（两处帮助弹窗白话条目） | 同上，指**5 类默认卡** —— 由同弹窗 **L470「方案里有 5 张卡，每张卡管一类业务」** 与 **L478「先看这单属于哪一类」**（→「项目、产品、办卡、充卡还是快捷开单，找对应的卡」）**自证**。属"业务类型"轴，**不是价目表归属** → **保持原文** |
| 「分类管理」 | 支出管理（`SortManageActivity`） | 支出记账的分类，与本模块无关 |
| 「分组筛选」「分组轨」「包含项目 / 包含产品」 | 价目表原型 | 本已用「组」，无需改动 |
| `category`（字段名） | 数据模型 | 只读兼容期保留（§4.3）；**字段名是接口契约，不在本次文案统一范围**（§12-2） |
| `primaryGroupId` / `groupIds` | 数据模型 | 新字段，本就是 group 命名 |

### 11.3 需现场确认（歧义项，暂不裁定）

| 表述 | 位置 | 两种读法 | 建议 |
|------|------|----------|------|
| 「该分类下暂无内容」 | `提成设置/comm2.js` **L2605**（选择页空态） | ① 指**分组轨**（价目表分组，则该改「该分组下暂无内容」）② 指**类型 Tab**（项目 / 产品 / 会员卡，则该不改） | 改用**中性文案**「**暂无内容**」—— 两种读法下都正确，且不引入新的轴歧义。**待确认后落地** |

> 该串触发条件为 `!items.length && !html`，即**当前视图（类型 + 分组筛选）无内容** —— 代码本身无法区分是"这个组是空的"还是"这个类型是空的"，故不做推测性改名。

### 11.4 执行方式

- **文案层**（§11.1）：替换即可，无逻辑影响；`comm2.js` 的三处字符串为**纯展示拼接**，改动不影响取数与比对。
- **文档层**：三份 PRD（本文 + `PRD-项目创建与管理.md` + `../提成设置/PRD-提成设置.md`）同步；历史修订记录条目**保留原文不改**（作为历史痕迹），仅在最新修订条中声明本次统一。
- **接口层**：字段名 / 接口键**本次不动**（用户决策：只改用户可见文案 + 文档口径）。`category` 的最终下线时机见 §12-2。
- **验收**：按 **M20**（改名命中）与 **M21**（排除项未被误改）双向核对。

---

## 12. 待确认 / 后续

1. **新增表单是否加「主分组」必选字段**：原型现状不给用户控件（写死「其他」/「产品」）。D1 要求主分组必选，建议新增表单加**主分组单选**（默认取上次使用，兜底 fallback），并在包含项目/产品页以「主分组」角标区分主分组与自定义分组。**待确认**。
2. **`category` 字段下线时机**：建议跨一个完整结算周期且 §9.3 核对全绿后；具体时点待排期。**字段名是否随术语统一改名（`category` → 语义化 group 命名）亦待定** —— 属接口契约变更，不在 §11 的文案范围内（§11.2）。
3. **分界记录的实现归属**：`_prevItemIds` / `effectiveAt` 写在哪一侧 —— 建议**分组对象自身**（§3.1），因为 `lineInGroup()` 已从 `comm2Groups()` 取分组对象，写入后**提成侧零改管线**；若价目与提成各自持有一份分组数据（`提成设置/seed-data.js` 的副本问题，§1.3 R7），须保证**单源**，否则分界会两边不一致。
4. **导入导出 / 商城·联盟侧**：D6 判定为不依赖，若有遗漏需回补映射。
5. **原型改码范围**：本文只定方案；`价目表/`（主分组 + 兜底组 + 删组保护 + 变更 toast）与 `提成设置/`（优先级链 + 生效来源可见化 + 分界解析 + 术语替换）的具体改动清单，待本方案评审通过后另行出「执行清单」。
